# WebDocSach Server

Server hiện chỉ phục vụ:

- file tĩnh của website
- API `GET /api/books` để trả danh sách truyện từ `data/books.json`

Chạy server:

```bash
npm start
```

Mặc định server chạy tại:

```text
http://localhost:3000
```
